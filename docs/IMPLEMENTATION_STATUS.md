# 实施状态

最后更新：2026-09-04（8000 吉客云订单三通道统一同步）

## 当前结论

- 本仓库为**按规格 MD 原栈（FastAPI + Next.js + Celery）的重写版**；此前的 TS monorepo（`~/Documents/ChatGPT/电商工作平台/`）Phase 0 已验证可用，用户决策于 2026-09-01 改回规格原栈重写，TS 版保留未删除。
- 实际运行仓库：`/Users/gino/ecommerce-workspace`，通过 LaunchAgent 原生运行于 **8000** 端口；不再依赖 Docker。
- 当前为**局域网 Web 访问 + 账号密码登录**：默认登录 12 小时，勾选“记住登录”后同一浏览器 30 天免重复输入；改密/退出立即撤销旧令牌。viewer 只读，operator/admin 可写。8000 不得端口转发到公网。
- 吉客云销售订单已统一为 **Web → Windows RPA → OpenAPI/MCP** 可配置故障切换：三个通道共用规范化、身份识别、去重、幂等写库和同步日志；运行入口为 `/api/v1/jky-orders/*`，订单来源与来源历史写入 `sales_orders`。当前生产仍为 `JACKYUN_SYNC_MODE=manual`，未验证的外部通道不会被显示为成功。

## Phase 状态

| Phase | 状态 | 说明 |
|---|---|---|
| Phase 0 工程基础 | 已完成 | 原生 LaunchAgent / PostgreSQL / Redis / Alembic / healthz / 审计 / .env 边界 / 45+ 表模型 / beat schedule / 单测 |
| Phase 1 吉客云 | **基础同步已运行** | MCP 传输与 14 个工具已验证；真实仓库、商品/SKU、库存、售后、近 7 天销售已入本地库。原始响应留档、分页和外部编号幂等已启用；价格接口本次为空，销售数量/精确下单时间未由当前接口返回，保持缺失而不伪造 |
| Phase 2 经营看板 | **核心完成** | 销售趋势/平台/SKU/库存/订单/售后均读取本地副本；商品页增加真实 SKU 主档表和搜索，销售页增加售后清单；空数据如实显示 |
| Phase 3 采购/1688 | **核心完成，1688 OAuth 外部阻塞** | 手工登记/上传真实 1688 订单后，采购分配必须选择已同步的吉客云 SKU，金额不平禁止确认；吉客云采购单必须真实存在才可关联。官方 1688 XLSX 导入使用实际 `alibaba1688_file_imports` / `alibaba1688_orders` 本地副本并按文件/订单幂等；采购全链路 `/procurement-chain` 已按 1688 订单号合并导出副本与采购工作流副本，在同一行展示采购内容/SKU、吉客云采购单、入库、结算、税票和认证；OAuth 订单拉取仍等官方应用凭证与接口权限，不伪造成功 |
| Phase 4 浙江农信+财务资料+税务清单 | **文件导入核心完成** | 银行 XLSX 先做 SHA256 版本化原件归档，再解析；税务系统官方发票 XLSX/CSV 进入独立批次、原始行和标准发票台账，支持方向/状态/金额/购销方识别，清单明确给出订单号时自动匹配采购/销售单；无法确认的行保留为待核对。银行回单 PDF 只归档，解析仍等真实样本。财务 ZIP V1/V2 不覆盖；SMTP 未配置如实失败 |
| Phase 5 回款+利润 | **核心完成** | 回款匹配与真实账号审计已上线。利润修正为按请求账期过滤、单位成本 × 销售数量；未映射 SKU、缺数量或缺成本时不输出伪精确商品成本/毛利。多来源成本可并存并按实际>采购>默认>暂估生效 |
| Phase 6 期初+异常+月结 | **核心完成** | 异常中心 API/页面可用；期初初始化向导（`/api/v1/opening/*`：7 类期初 + 差异池 + 审计日志）已上线（设置页）；月结快照 `/api/v1/closing/*`（V1 保留、重算产生 V2/V3，快照含毛利/回款指标）已上线（利润页）；自动化页改为展示真实 beat schedule + 同步任务/日志；`monthly_verify` 月初完整校验已实现（缺资料自动进异常中心）；SMTP 发送仍阻塞 |

## 外部阻塞

| 项目 | 状态 | 所需动作 |
|---|---|---|
| 吉客云 MCP | **已连接并基础同步** | 凭证仅保存在服务端 `.env`（未入 Git）；2026-09-02 已取得真实业务数据。单次销售查询最多 7 天，单页最多 10 条；未返回字段保持空值 |
| 1688 | 阻塞 | 创建开放平台应用 → 提供 AppKey/Secret + 回调地址 |
| 浙江农信解析 | XLSX 可用 / PDF 部分阻塞 | XLSX 已可归档+解析；旧 XLS 需另存为 XLSX；提供脱敏银行回单 PDF 样本后再实现回单识别 |
| SMTP | 阻塞 | 提供邮件服务账号 + 收件人 + 人工确认规则 |

## 验证记录

### 2026-09-04 采购链路人工匹配闭环（8000）

- 吉客云入库单继续保存在本地业务副本表中；本次操作前已生成 `backups/db_20260904_001252.dump` 与 `backups/data_20260904_001252.tar.gz`，没有删除任何入库原始单或明细。
- 修复旧规则“供应商名一致即自动确认入库”的严重误关联：精确移除 1,887 条由该旧规则生成的自动确认链接，86 张入库原单保持不变；37 条有效 1688 订单当前确认入库数恢复为 0。
- 新匹配规则只在“入库单明确记录 1688 订单号”，或“供应商、金额一致且 45 天内时间最近”时生成候选，而且候选一律为**待人工确认**。当前真实数据生成 13 条候选，未自动确认任何一条。
- `/purchase/workbench` 订单详情新增“入库单匹配”：可搜索本地入库单库、查看单号/供应商/仓库/日期/金额/匹配依据，支持人工关联、把已关联记录更换到另一张入库单、解除关联；一张订单可按实际情况关联多张入库单。
- 拒绝或解除的组合保留为审计状态，不再进入待确认统计，也不会在下一次生成建议时被反复推荐；用户仍可在候选列表中人工重新选择。
- API 新增 `GET /procurement-chain/candidates` 与 `PUT /procurement-chain/links/{id}`；已确认关联返回 `linkId/targetId/matchMethod/amount/supplier`，前端可以精确操作具体关联，而不是只显示文本。
- 清理 8000 遗留孤立 Uvicorn 进程后，由 LaunchAgent 正常托管单一 API 进程；本机 `/healthz` 返回 `ok=true`，监听进程的父进程为 `scripts/native-start.sh`。
- 验证：`make test` **158 passed**、`make lint`、`make tsc`、`make migration-check`、前端 `npm run build`、`make smoke`（68 个 GET 路由，0 fail）全部通过。

### 2026-09-04 采购 SKU 人工匹配与成本确认（8000）

- 在采购工作台新增二级视图：`/purchase/workbench?view=matching`。上半部分处理“吉客云入库明细 ↔ 吉客云 SKU”，下半部分处理“1688 订单 ↔ SKU 分配”；不会把采购流程拆成互不关联的页面。
- 入库明细支持人工指定、替换或解除已匹配 SKU；解除后仍为 `manual` 待处理状态，后续自动扫描只补空缺/自动结果，不会把人工解除的明细重新绑定。1688 待配置订单提供候选 SKU、数量和单价录入，保存后可进入订单调整继续变更或删除分配。
- 当前生产副本共 159 条入库明细：25 条价格正常、119 条自动匹配、14 条人工锁定、1 条价格异常，未匹配和缺金额均为 0。现存 14 条人工锁定结果予以保留；旧审计表仅有 1 条历史成本确认日志，因此不将未留痕的旧记录误判为系统自动结果。之后的人工匹配、自动扫描和成本确认都会写审计日志。
- 成本不会因自动扫描写入 SKU 主档。前端弹窗确认之外，API 还要求 `confirm=true` 的显式提交；已关联 1688 订单时按实付/入库单总额比例分摊，未关联时才取入库含税单价。确认后的 `default_cost` 变更会留审计记录，并影响后续利润计算。
- 仍有 1 条异常故意不自动接受：吉客云入库单 `RK202601270001` 的“卖咖啡的熊 NS 法式焦糖风味 1号”，推算单价 ¥85.0000，相对同 SKU 历史中位数 ¥8.8 偏差超过 50%。界面会阻止写入主档并提示先核对吉客云单据金额或数量。
- 验证：SKU 匹配专项测试 7 passed；完整后端回归 `make test` 165 passed，`make lint`、`make tsc`、`make migration-check`、前端 `npm run build` 通过。8000 已发布，`/healthz` 返回 `ok=true`；带鉴权 `make smoke` 枚举 69 个 GET 路由，51 个 200、18 个预期 4xx、0 个失败。线上还验证了未显式确认成本返回 400，解除匹配路由已发布。

### 2026-09-02 14 工具维度补齐（原生运行）

- 新增迁移 `g9e2f5b7c1d3`、`h1f6a8c3d5e7`：采购结算、采购退货、入库/出库单及明细、库存调拨、网店订单及商品明细均有独立本地副本表；所有表保留 `raw`，外部编号幂等。
- 重启 LaunchAgent 后手动强制验证新增任务：入库 13 单/18 条明细、出库 167 单/339 条明细、网店订单 17 单/12 条商品明细；采购结算、采购退货、库存调拨本次接口返回 0 条，任务状态为 success 而非伪造业务数据。
- OMS 订单接口本次 23 小时窗口返回 0 条；适配器已按真实 `tradeNo/createTime/payTime/goodsDetailList` 字段落库，后续有数据会补上订单时间、数量和明细。
- `make migrate`、`make migration-check`、`make test`（138 passed）、`make lint`、`make tsc`、`make smoke` 全部通过；API `/healthz` 返回 `ok=true`，LaunchAgent active count=1。

### 2026-09-02 采购全链路部署（8000）

- `/procurement-chain` 已部署到原生 8000 服务；链路主表保持“一行一条 1688 订单”，新增采购内容/SKU、吉客云采购单和采购工作流状态列，完成度按 7 个业务节点统计。
- 已将现有 100 条 `alibaba1688_orders` 幂等回填到 `external_purchase_orders`，只补来源订单主档，不覆盖 SKU 分配、费用、状态或原始文件；后续重复上传仍按订单号更新来源字段。
- 当前真实数据口径：100 条 1688 订单、180 条吉客云出入库单、0 条吉客云采购结算、0 条税务发票；未导入/未明确关联的资料在页面保持“待完善/待关联”，不伪造已完成链路。

### 2026-09-02 新版采购链路台账

- 新增独立页面 `/procurement-ledger`，保留旧 `/procurement-chain` 不改；新版采用“左侧订单台账 + 右侧链路详情”，每个 1688 订单作为一条主记录，完整查看 SKU、吉客云采购单、入库、付款、税票和认证。
- 新版页面内置订单搜索、待办筛选、7 环节完成度、关联建议确认/拒绝、发票认证切换和三个资料导入入口；吉客云采购单环节明确标注“一对一 / 待关联 / 需核对”。
- 前端 `npm run build`、`make tsc` 和 `git diff --check` 已通过；新页面由原生 8000 同口托管。

### 2026-09-02 采购工作台截图布局

- 新增采购下的二级“工作台”入口，路径 `/purchase/workbench`（同时保留 `/procurement-board` 兼容地址），采用截图布局：顶部 6 个统计卡、左侧按时间分组订单队列、右侧订单详情与流程状态。
- 详情区补充供应商档案、常购 SKU、当前订单吉客云 SKU 分配、采购单/入库、税票和金额付款分层；顶部导入、新建和导出操作均有明确入口，未配置的外部能力不显示为成功。

### 2026-09-01 Phase 0（重写版）

- `docker compose config` 通过；postgres:17.11-alpine / redis:7.4-alpine（本机已有镜像，Docker Hub 直连被墙，python 基础镜像经 daocloud 镜像拉取）
- Alembic 初始迁移 autogenerate 生成并 upgrade head（见迁移文件 0001）
- 单元测试：金额 Decimal（拒绝 float）、采购分配平衡校验（规格 7.4 示例 50000 分配）、费用分摊尾差归零、银行文件 SHA256/文件名消毒 —— 全部通过
- 旧 Docker Web 端口：从旧 TS 版切换到新栈后验证（已废弃）

### 吉客云连接测试结果（2026-09-01 真实调用）

| 步骤 | 结果 |
|---|---|
| MCP 传输 | ✅ Streamable HTTP POST `/mcp/messages`；认证为 `Authorization: <裸Token>`（**不带 Bearer 前缀**，实测 8 种 header 变体确认） |
| initialize | ✅ serverInfo = `open-platform-mcp v1.0.0` |
| tools/list | ✅ 返回 14 个工具，与订阅清单 1:1 对应（tool 名与 inputSchema 见 `docs/jackyun-samples.json`） |
| tools/call（业务数据） | ❌ **真实阻塞**：`code 0130000609 "该应用未开通开放平台，无法调用接口，请联系客户经理处理"` |

**用户下一步最小动作：联系吉客云客户经理，为企业应用开通"开放平台"API 调用权限**（MCP 授权已完成，差业务侧开通）。开通后无需改代码，重测即可拉到真实字段样本。

### 验证记录（补充）

- 单元测试 14/14 通过（Decimal 拒绝 float、分配平衡、分摊尾差归零、SHA256/文件名消毒）
- 旧 Docker 本机 + 局域网入口均 200；6 服务全部 healthy（该运行态已废弃）
- 前端设置页连接测试可复现 MCP 传输成功；整体状态徽章保持“业务权限未开通”，不再显示为已连接

### 2026-09-01 Phase 5（回款 + 利润）

- 后端：`services/reconciliation.py`（匹配框架/规则/指纹/确认）、`api/v1/reconciliation.py`、`services/profit.py`（成本优先级/毛利诚实语义）、`api/v1/profit.py`；路由已挂载 /api/v1/reconciliation/*、/api/v1/profit/*
- 单测 47/47 通过（新增回款 8 项 + 利润 8 项：成本优先级、毛利 None 语义、多来源校验、指纹稳定/摘要参与 fallback）
- 前端：回款页（指标卡/规则管理/应收登记/匹配建议确认拒绝）、利润页（成本登记/覆盖统计/毛利计算）接真实 API，无模拟数据
- 银行流水登记与 XLSX 导入在回款页；PDF 回单识别仍等脱敏样本

### 2026-09-01 Phase 2 + Phase 6（经营看板 / 期初 / 月结）

- 后端新增：`services/dashboard.py` + `/api/v1/dashboard/*`（销售趋势/平台排行/SKU 排行/库存/订单/售后）；`services/opening.py` + `/api/v1/opening/*`（7 类期初 + 差异池 + 调整审计）；`services/closing.py` + `/api/v1/closing/*`（月结快照 V1 保留、重算产生 V2/V3）；`/api/v1/automation/*`（beat schedule/任务/日志展示）
- `system.overview` 9 指标改为本地库真实聚合（空态如实 None），phase 更新为 6
- 吉客云 14 个 MCP 工具均有同步入口；采购结算、采购退货、入库、出库、库存调拨、网店订单写入独立本地副本表（raw 存档 + 外部编号幂等），`tasks.sync_jackyun` 与 beat 均覆盖；未知字段保持空值，不猜业务含义
- 前端：销售页/商品与库存页/自动化页接真实 API；设置页加期初初始化向导；利润页加月结快照区
- 新增单测 6 项（金额平衡规格示例/分摊尾差/float 拒绝）—— 纯逻辑层

### 2026-09-01 Phase 3/4 补强（1688 OAuth 骨架 + XLSX 解析 + 邮件发送链路）

- 1688：`GET /integrations/alibaba1688/auth-url`（未配置如实 400）+ `GET /integrations/alibaba1688/callback`（未配置返回 waiting_config 并进异常中心）；设置页「连接 1688」入口
- 银行解析：`bank_file.parse_xlsx` 通用列名检测（交易日期/摘要/对方户名/收入/支出/余额/流水号，支持多种日期格式，非 XLSX/无关键列返回空）；`reconciliation/import-bank` 上传导入（指纹幂等）
- 邮件：`finance_service.send_delivery`（SMTP 未配置如实报错；一个账期+版本仅一条 first 成功，重发标记 RESENT；写 EmailDeliveryLog + audit）；`POST /finance/{y}/{m}/send` + `GET /finance/delivery-logs`；财务页「发送给财务」按钮
- E2E 实测：合成 XLSX 导入首次 2 条新增 → 重复导入 0 新增（幂等）✅；send 无 SMTP 返回 400 明确提示 ✅
- 单测 59/59 通过（新增 6 项：XLSX 列检测/日期格式/合计行跳过/非交易表/空内容/发送 kind 逻辑）

### 2026-09-02 税务系统官方清单台账

- 新增 `j2a7c9e4f6b8` 税务清单迁移和 `k3b8d1f5a7c9` 合并迁移：原件 `tax_invoice_imports`、逐行原始记录 `tax_invoice_import_records`、标准台账 `tax_invoices`、可审计关联 `tax_invoice_links`。
- 支持税务系统导出的 `.xlsx` / `.csv`：自动寻找发票表头，识别发票代码/号码、类型、开票日期、购销方及税号、金额/税额/价税合计、方向、状态和关联单号；同一文件按 SHA256 幂等，原件保存到 `data/tax-invoices/`。
- 自动匹配仅接受清单明确提供的关联订单号；金额/名称相似但没有明确编号的记录不自动猜测。未识别行不丢弃，在“税务发票”页以“待核对”显示。
- 前端入口：`/tax-invoices`；采购详情同时显示已由官方清单明确匹配的税票。税票台账为空属于“尚未上传清单”，不是“全部未开票”。
- 验证：`make test` **142 passed**、`make lint`、`make tsc`、前端 `npm run build`、`make migration-check` 均通过。

## 技术决策记录

1. **重写回规格原栈**（2026-09-01，用户决策）：TS 版与规格 MD 栈不一致；用户确认趁 Phase 0 早期重写成本低。
2. **局域网密码登录**：保留 LAN 访问范围，但取消免账户信任模式；30 天免输入由可撤销的登录令牌实现。
3. **镜像策略**：Docker Hub 直连超时 → 复用本机已拉取镜像标签 + daocloud 镜像拉取 python:3.12-slim + pip 走清华源。
4. **MCP 传输**：按 Streamable HTTP（POST JSON-RPC 至 `/mcp/messages`，吉客云要求裸 Token 认证，`Mcp-Session-Id` 会话保持）；如真实响应异常将记录于上文验证记录。

## 下一步

- 在自动化页观察下一轮吉客云任务，确认分页增量同步持续成功；需要更早历史销售时按吉客云 7 天窗口分段补拉
- 价格接口当前返回空列表；待吉客云端维护价格后重跑 `price_lists`
- 销售接口当前未回传明细数量和精确下单时间，保留原始响应并在看板/利润中明确缺失；后续以官方导出或新增获准字段补齐
- Phase 4 剩余：银行回单 PDF 识别（等脱敏样本）、SMTP 配置后开放发送链路（first/resent 记录已建）
- 税务清单后续：上传第一份真实官方 XLSX/CSV 后核对表头 mapping；若税务导出没有关联订单号，业务关联保持待核对，不用模糊匹配冒充完成
- Phase 3：1688 应用创建后实现 OAuth 回调与订单拉取
- Phase 5 剩余：销售数据（吉客云）落地后校验毛利计算；贡献利润待费用数据可靠后开放
- 自动化频率后台可配置化（当前 beat schedule 频率固定，配置界面列入下一迭代）

### 2026-09-04 采购工作台统一数据接入与人工闭环

- 采购工作台成为统一入口：旧业务页保留兼容地址但进入同一工作台；左侧三项“1688 订单导入 / 吉客云导入 / 税务发票”收敛为单一“数据接入”。
- 数据接入以三个明确标签管理原始文件：1688 订单、吉客云业务单据（采购单/入库单/结算单）、税务发票清单。文件上传仍默认待人工核对再生效；在线接口同步单独收纳在顶部“在线同步”，不与文件导入混淆。
- 订单详情的补资料入口会携带当前订单上下文；导入后可以返回同一订单继续手动关联。SKU 可新增、修改、删除或重新打开已确认采购内容；采购单、入库单、结算单、税务清单发票均支持人工关联、更换或解除，原始资料不删除。
- 新增迁移 `s1e5f8a2b4c6`：工作流独有的手工采购记录也可关联真实入库/结算单，工作台使用负数内部 ID 避免与 1688 文件订单主键碰撞；列表聚合会对同一结算单去重，避免重复计入已付款。
- 入库先于采购工作流建立的历史顺序已补齐：工作流创建/1688 文件确认后，会按已有确认入库单的已匹配 SKU 幂等反填采购分配；列表状态将“采购内容待确认”和“SKU 待匹配”分开显示，避免已匹配 SKU 仍被误标为待匹配。
- 采购分配规则进一步收敛：入库明细已匹配真实 SKU 且金额平衡时自动确认采购内容；只有未匹配 SKU 或金额/数量不平才进入人工处理。负金额差异在界面明确标为“超分配/金额异常”。
- 发票登记改为原子操作：校验失败不留下孤立发票；只能登记已有真实发票号码，税务认证按钮只记录“已在税务系统完成认证”的本地结果，不会伪造外部开票或认证。
- 新增工作台内“待关联分拣中心”：以一张 1688/工作流订单为主线，同时列出吉客云采购单、入库单、结算单和税务进项发票候选；候选按照供应商、单号、金额、日期排序供人工判断，只有点击“关联”或确认高置信的既有建议才会写入正式关联，不做名称相似即自动绑定。
- 验证：数据库迁移和 `alembic check` 通过；`make lint`、`make tsc`、前端 `npm run build`、`make test` **167 passed**、`make smoke`（70 个 GET，52 个 200、18 个预期 4xx、0 fail）通过。8000 已由 LaunchAgent 重启，`/healthz` 返回 `ok=true`。测试已改为使用随机隔离数据，不再假定真实税务台账为空。

### 2026-09-01 登录与 RBAC 上线（原"局域网信任模式"关闭）

- 后端：全局鉴权依赖 `api/deps.py`（除 `/auth/login`、1688 OAuth 回调外全部 `/api/v1` 需令牌）；`core/auth.py` PBKDF2 口令散列 + HMAC-SHA256 签名令牌（12h 有效，纯标准库实现）
- 接口：`POST /auth/login`、`GET /auth/me`、`POST /auth/change-password`、`GET|POST /auth/users`（admin 建号）；登录成功/失败/改密/建号均写审计日志
- 数据库：迁移 `a1f2c3d4e5f6` 为 users 加 `hashed_password`；启动自动种入角色 admin/operator/viewer 与初始管理员（env：ADMIN_USERNAME / ADMIN_PASSWORD，已存在不覆盖，FORCE_ADMIN_PASSWORD=1 强制重置）
- 前端：`/login` 登录页 + 服务端令牌校验守卫 + 侧栏用户名/角色/服务端退出 + 401 统一跳转；财务 ZIP 改为带 Authorization 的 Blob 下载，令牌不再进入 URL/日志/历史记录
- 豁免路径：`/healthz`（探针）、`/api/v1/auth/login`、`/api/v1/integrations/alibaba1688/callback`（外部重定向无请求头）
- 验证：未登录/伪令牌 401；URL query 令牌拒绝；admin 登录；viewer 写操作 403；改密/退出撤销旧令牌；worker/beat 不受影响
- ACCESS_MODE 已切 rbac；system/overview 如实返回 rbac（原硬编码 lan_trusted 已修）

### 2026-09-01 记住登录（免输入）

- 登录接口新增 `remember_me`：勾选签发 30 天令牌，默认 12 小时；登录页默认勾选"记住登录（此电脑 30 天内免输入）"
- 令牌存 localStorage（浏览器关闭不丢失），30 天内同一电脑浏览器免重复输入；取消勾选则仅记住用户名不记令牌
- 验证：remember_me=false 令牌 12 小时 / true 令牌 30 天；长令牌可访问，退出后立即失效
- 注意：公共电脑请取消勾选，并在侧栏"退出"清理本机令牌

### 2026-09-02 历史 Docker 运行态巡检与修复验证（已废弃）

- 运行：`api`、`worker`、`beat`、`frontend`、PostgreSQL、Redis 均运行；本机和局域网健康检查均返回 `{"ok":true,"accessMode":"rbac"}`。应用容器以本机文件所有者 UID/GID 运行，不再以 root 运行 Celery；启动日志无 root 安全警告或未处理异常。该 Docker 运行态现已由原生 8000 运行替换。
- 登录与权限：登录页可见用户名/密码和默认勾选的“记住登录（此电脑 30 天内免输入）”；未登录访问受保护页面会回到 `/login`。运行时枚举 31 个静态 `/api/v1` GET 路由，结果为 30 个 401、1 个缺参数 422、0 个匿名 2xx。令牌 query string 被拒绝；改密/退出会通过 token version 撤销旧令牌。
- 外部状态真实性：真实调用吉客云 MCP 的 `initialize → tools/list` 得到 14 个工具；业务接口返回 `0130000609` 时，任务为 `failed`、连接为 `blocked`、`businessReady=false`，不会写入假数据。1688 在未配置 AppKey/Secret 时显示 `unconfigured`；仅 MCP 通道成功但尚未业务验证时显示 `transport_connected`，不会显示“已连接”。
- 数据保护：业务数据表（商品、SKU、销售、售后、采购、结算、利润）仍均为 0，未因巡检生成测试业务数据；已有银行流水 2 条、期初 2 条、财务期间 1 条、财务包 1 条、归档文件 3 条保持。部署前备份保留在 `backups/db_20260901_201527.dump` 与 `backups/data_20260901_201527.tar.gz`。
- 迁移：`e7b4c2d19a01`（users.token_version）已在数据库 head；`alembic check` 无漂移。
- 自动验证：`make test` **113 passed**；`make lint`、`make tsc`、`make migration-check`、`make smoke` 全部通过。测试输出仅保留 FastAPI/Starlette 对未来 TestClient 依赖迁移的第三方弃用提示，不影响运行结果。

### 2026-09-02 原生运行与吉客云 MCP 基础同步

- 实际服务由 LaunchAgent `com.gino.ecommerce-dashboard` 托管，API 和静态 Web 共用 `0.0.0.0:8000`；本机 PostgreSQL、Redis、worker、beat 均已找到并在运行。
- MCP `initialize → tools/list` 成功，返回 14 个已订阅工具；`tools/call` 已从原 `0130000609` 权限阻塞转为可返回真实业务数据。
- 同步器已按真实多层 MCP 响应、`goodsNo`、`skuBarcode`、`warehouseCode`、`currentQuantity` 修正，支持最大 10 条/页分页、外部编号幂等和原始响应留档；销售查询使用小于 7 天的窗口。
- 首次基础同步结果：4 个仓库、88 个商品档案、35 个 SKU、25 条库存快照、66 笔销售订单（76 条明细，全部已关联 SKU）、109 笔售后；所有同步任务均为 success。
- 数据质量边界：价格接口返回 0 条；销售接口没有回传 `sellCount` 和精确下单时间，订单明细数量及 `ordered_at` 保持 NULL，不以金额反推伪造数据。
- 原生备份脚本已可用并于同步后生成 `backups/db_20260902_152334.dump` 与 `backups/data_20260902_152334.tar.gz`；本次运行设为保留 999 份，未清理旧备份。
- 验证：后端测试 134 passed；`alembic check` 无迁移漂移。

### 2026-09-04 耗材库与成本策略

- 新增耗材主档 `consumables`：耗材代码/条码唯一、名称、分类、单位、采购单价、期初/当前库存和低库存标记。
- 新增 `consumable_sku_mappings`：按货品 SKU 配置每单位耗材用量；未配置映射时系统不会猜测或虚构消耗。
- 新增 `consumable_transactions`：采购入库、领用消耗、盘点调整三类库存流水；耗材领用改为跟随吉客云入库单确认，来源记录为入库关联并幂等，避免采购内容确认时提前扣库存。
- 货品档案增加成本方式：`fixed`（默认，使用 `default_cost`，默认容差 2%）或 `dynamic`（使用实际入库/结算成本）。固定模式单价异常会进入异常中心并阻止自动确认，动态模式不以旧固定成本误报。
- 动态模式的入库明细会按入库账期写入 `CostSnapshot.actual_cost`，利润中心继续按既有成本优先级读取；固定模式仍以货品档案固定成本为核对基准。
- 商品与库存页新增“货品档案 / 耗材库”二级页签，可维护耗材、映射和库存流水；原 Excel 的 `订货` 与 `耗材使用情况` 可按代码和单位消耗量迁移，原表中的错误公式不作为真实数据。
- 新增迁移 `t2c4e6a8b0d1`、`u3d5f7b9c1e2`；迁移、后端测试、前端类型检查和构建均通过。
- 货品档案支持“新建货品”和逐行“修改”：可修正 SKU 名称、条码、单位、售价、固定成本、成本方式和容差；新建记录未提供吉客云 ID 时使用 SKU 编码作为本地唯一标识并标记为本地维护。
- 已导入 `/Users/gino/Desktop/卖咖啡的熊-OEM-咖啡豆&胶囊咖啡.xlsx`：新增 14 条耗材、自动建立 15 条明确 SKU 映射；其中原表采购单价为空的记录保持为空，不用 0 或估算值填充。
- 货品档案按 `ES` 前缀区分套装与单品（当前自动识别 51 个 ES 套装 SKU），页面支持人工调整类型；耗材页拆为“耗材库”和“耗材使用进度”两个独立 Tab，进度显示采购量、已使用量、剩余量和使用率。
- 入库联动新增耗材使用确认：每张吉客云入库单关联时必须明确“有耗材使用”或“本次不使用耗材”；选择使用时必须填写每种耗材数量，确认后才扣减库存并写入 `inbound_consumable_usages` 与库存流水。更换/解除入库关联会先冲销原耗材扣减；采购工作台推进到“已入库/完成”前会阻止未确认耗材使用的订单。
- 采购工作台订单详情已加入耗材使用面板：按入库单候选的已匹配 SKU 与耗材映射自动带出建议数量，支持人工修改、添加或移除耗材；旧入库关联显示“待确认耗材使用”，可通过“更换”补录。
